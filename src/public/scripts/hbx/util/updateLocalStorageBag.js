/* global LIST_OF_EDITED_ITEMS, fetchRemoveCartItem, fetchUpdateBag, window */
/* exported updateBag */

// const updateLocalStorageBag = () => {

  // let array = LIST_OF_EDITED_ITEMS;
  //
  // Promise.all(array.map( (obj) => {
  //   if(obj.count < 1){
  //     return fetchRemoveCartItem(obj);
  //   }
  //   return fetchUpdateBag(obj);
  // }))
  //   .then(() => {
  //     window.location.reload(true);
  //   })
  //   .catch( err => {return err;} );

// };
